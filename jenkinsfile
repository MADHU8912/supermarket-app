pipeline {
    agent any

    environment {
        IMAGE = "nikhil123/supermarket"
        TAG = "latest"
    }

    stage('Test Docker') {
    steps {
        bat "docker --version"
        bat "docker ps"
    }
}